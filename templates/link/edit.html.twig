{% extends 'layout.html.twig' %}

{% block body -%}
    <div class="container mt-4">
        <h1 class="display-4">Link bearbeiten</h1>
        <div class="mt-4">
            <div class="card">
                <div class="card-body">
                    {{ include('link/link_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}